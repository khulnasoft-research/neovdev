# Template source apps

These apps are source material for generated project templates.

- `web-chat-next-template` generates `packages/ovo/src/setup/scaffold/create/web-template.ts`.

Edit the source app first, then run `pnpm --filter ovo generate:web-template` to refresh generated scaffold output.
